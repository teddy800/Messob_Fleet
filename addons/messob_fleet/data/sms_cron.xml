<?xml version="1.0" encoding="utf-8"?>
<!--
    MESSOB Fleet Management System — SMS Cron Jobs
    
    Scheduled tasks for SMS notifications:
    - Retry failed SMS deliveries
    - Clean up old SMS logs
-->
<odoo>

    <!-- Retry Failed SMS (every 30 minutes) -->
    <record id="cron_retry_failed_sms" model="ir.cron">
        <field name="name">FMS: Retry Failed SMS</field>
        <field name="model_id" ref="model_messob_fms_sms_log"/>
        <field name="state">code</field>
        <field name="code">model._cron_retry_failed_sms()</field>
        <field name="interval_number">30</field>
        <field name="interval_type">minutes</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
    </record>

    <!-- Clean Up Old SMS Logs (daily at 2 AM) -->
    <record id="cron_cleanup_old_sms_logs" model="ir.cron">
        <field name="name">FMS: Clean Up Old SMS Logs</field>
        <field name="model_id" ref="model_messob_fms_sms_log"/>
        <field name="state">code</field>
        <field name="code">model._cron_cleanup_old_logs()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=2, minute=0, second=0)"/>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
    </record>

</odoo>

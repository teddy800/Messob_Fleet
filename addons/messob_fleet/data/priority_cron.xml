<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Cron job to recalculate trip priorities every hour -->
    <record id="cron_recalculate_trip_priorities" model="ir.cron">
        <field name="name">MESSOB FMS: Recalculate Trip Priorities</field>
        <field name="model_id" ref="model_messob_fms_trip"/>
        <field name="state">code</field>
        <field name="code">model._cron_recalculate_priorities()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
    </record>
</odoo>
